<?php
if( function_exists('acf_add_options_page') ) {
	// Adds an options page with your site title
    acf_add_options_page(array('page_title' => "Contact Details", 'position' => 1.1,));
}

/* Not in use at the moment */